# Requisitos para Coppel Sales Analysis (Python)
# Instalar con: pip install -r requirements.txt

pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0  # Para leer archivos .xlsx
xlrd>=2.0.0      # Para leer archivos .xls (legacy)
